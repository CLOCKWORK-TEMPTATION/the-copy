

## TODO LIST – إصلاح مشروع The Copy Monorepo وجعله جاهزًا للبناء

### سياق مختصر (للوكيل)

* المشروع: Monorepo يحتوي على:

  * `frontend` (Next.js + TypeScript)
  * `backend` (Node.js/Express + TypeScript)
* المشكلة الحالية: المشروع غير قابل للبناء بسبب أخطاء في مسارات الاستيراد وبعض مشكلات التنظيم والأنواع.

---

### القسم الأول: إصلاح المسارات المكسورة (أولوية قصوى)

1. **TODO-1: إصلاح مسار `shared/schema` في الملفات الستة الحرجة**

   * في كل ملف من الملفات التالية استبدل كل ظهور لـ:

     ```ts
     from "../shared/schema"
     ```

     إلى:

     ```ts
     from "./shared/schema"
     ```
   * الملفات:

     * `frontend/src/lib/api.ts`
     * `frontend/src/app/(main)/directors-studio/shots/page.tsx`
     * `frontend/src/app/(main)/directors-studio/script/page.tsx`
     * `frontend/src/app/(main)/directors-studio/scenes/page.tsx`
     * `frontend/src/app/(main)/directors-studio/lib/api.ts`
     * `frontend/src/app/(main)/directors-studio/characters/page.tsx`
   * نفّذ التعديل ملفًا بملف، وكلما انتهيت من ملف اعتبره منجزًا على شكل رسالة حالة داخلية:
     `✓ تم إصلاح frontend/src/lib/api.ts` … وهكذا لباقي الملفات.

2. **TODO-2: التحقق اليدوي من صحة المسار بعد التعديل**

   * افتح كل ملف من الملفات الستة بعد التعديل.
   * تأكد أن ملف `shared/schema` موجود فعليًا في نفس المجلد (نفس مستوى الملف) بحيث يكون المسار النسبي `./shared/schema` صحيحًا.
   * إن لم يكن الملف موجودًا في نفس المستوى، عدّل المسار وفق البنية الفعلية للمجلد مع الحفاظ على مبدأ: **إزالة الخطأ في الـ module resolution نهائيًا**.

---

### القسم الثاني: توحيد مسارات الاستيراد باستخدام Path Aliases

3. **TODO-3: توحيد الاستيرادات في الواجهة الأمامية Frontend**

   * نطاق المراجعة:

     * `frontend/src/lib/**`
     * `frontend/src/app/**`
     * أي مجلدات مشتركة أخرى داخل `frontend/src`.
   * المطلوب:

     * استبدال المسارات النسبية العميقة مثل:

       * `../../../lib/...`
       * `../../components/...`
     * بالـ aliases المعرّفة في `tsconfig.json`، مثل (أمثلة):

       * `@/lib/...`
       * `@/components/...`
       * `@shared/...` (إن كان مُعرّفًا).
   * احرص على:

     * عدم تغيير المنطق البرمجي.
     * الإبقاء على نفس الوحدات المستوردة، مع تعديل المسار فقط.
     * الحفاظ على اتساق النمط في الملف الواحد وفي المجلد الواحد قدر الإمكان.

4. **TODO-4: توحيد الاستيرادات في الخلفية Backend**

   * نطاق المراجعة:

     * `backend/src/services/**`
     * `backend/src/types/**`
     * باقي المجلدات في `backend/src` التي تستخدم مسارات نسبية عميقة.
   * المطلوب:

     * استبدال المسارات النسبية مثل:

       * `../../services/...`
       * `../types/...`
     * بالـ aliases المعتمدة، مثل (أمثلة):

       * `@/services/...`
       * `@/types/...`
   * تأكد من:

     * توافق المسارات مع إعدادات الـ `tsconfig.json` في الـ backend.
     * بقاء الـ imports صالحة بعد التعديل (لا تُدخل مسارات غير موجودة).

---

### القسم الثالث: التحقق من البناء والأنواع ومعالجة الأخطاء

5. **TODO-5: تثبيت الاعتماديات وتشغيل فحص الأنواع والبناء – Frontend**

   * انتقل إلى مجلد `frontend`.
   * نفّذ الأوامر بالترتيب:

     ```bash
     pnpm install
     pnpm typecheck
     pnpm build
     ```
   * إن ظهر أي خطأ:

     * سجّل اسم الملف، رقم السطر، ورسالة الخطأ نصًا كاملًا.
     * عدّل الشيفرة لإزالة الخطأ **دون تغيير المنطق البرمجي** قدر الإمكان.
     * أعد تشغيل الأمر الذي فشل حتى ينجح بلا أخطاء.
   * اعتبر المهمة مكتملة فقط عندما:

     * يمرّ `pnpm typecheck` بلا أخطاء.
     * يمرّ `pnpm build` بلا أخطاء.

6. **TODO-6: تثبيت الاعتماديات وتشغيل فحص الأنواع – Backend**

   * انتقل إلى مجلد `backend`.
   * نفّذ الأوامر:

     ```bash
     pnpm install
     tsc --noEmit
     ```
   * إن ظهرت أخطاء TypeScript:

     * سجّل الملفات المتأثرة، أرقام الأسطر، ورسائل الأخطاء كاملة.
     * أصلح تعريفات الأنواع، مسارات الاستيراد، أو التواقيع التي تسبب الفشل.
     * أعد تشغيل `tsc --noEmit` حتى ينجح بلا أي خطأ.

7. **TODO-7: فحص نهائي شامل للأنواع والمسارات**

   * بعد إكمال TODO-5 و TODO-6 بنجاح:

     * أعد تشغيل:

       * `pnpm typecheck` في `frontend`
       * `tsc --noEmit` في `backend`
   * الهدف: تأكيد اختفاء جميع أخطاء الترجمة والـ module resolution نهائيًا في المشروع ككل.

---

### القسم الرابع: فحص الاعتماديات الدائرية (Circular Dependencies)

8. **TODO-8: تحليل الاعتماديات الدائرية في المسارات الحرجة**

   * نطاق الفحص:

     * `frontend/src/lib/drama-analyst/orchestration/`
     * `frontend/src/lib/api.ts`
     * `backend/src/services/`
   * المطلوب:

     * اكتشاف أي حالة يكون فيها:

       * الملف A يستورد من B
       * والملف B يستورد من A مباشرة أو عبر سلسلة (A → B → C → … → A).
   * عند اكتشاف دورة:

     * اقترح أو نفّذ إعادة تنظيم للاستيرادات (مثل نقل أنواع مشتركة إلى ملف مستقل، أو فصل الوظائف المشتركة في وحدات محايدة) لكسر الحلقة مع الحفاظ على السلوك الوظيفي.

---

### القسم الخامس: التوثيق والتقرير النهائي

9. **TODO-9: إنشاء ملف توثيق `FIXES_APPLIED.md` في جذر المستودع**

   * أنشئ الملف في جذر المشروع (root) بالمحتوى التالي على الأقل:

     * قائمة بالملفات التي تم تعديلها.
     * نوع التعديل في كل ملف (مثال: "تصحيح مسار استيراد"، "استبدال مسار نسبي بـ alias"، "إصلاح نوع TypeScript").
     * حالة الـ build النهائية:

       * نتيجة `pnpm typecheck` و`pnpm build` في `frontend`.
       * نتيجة `tsc --noEmit` في `backend`.
     * قائمة مختصرة بأي مشاكل متبقية (إن وجدت) مع وصف سبب عدم حلّها في هذه الجولة.

10. **TODO-10: الالتزام برسائل Git واضحة (اختياري إن كان Git متاحًا)**

    * إن كانت بيئة التنفيذ مرتبطة بـ Git:

      * نفّذ Commits منطقية مثل:

        * `fix: correct import paths in directors-studio files`
        * `chore: unify import aliases in frontend`
        * `chore: fix ts types in backend`
      * حافظ على فصل التغييرات الكبيرة في Commits مستقلة قدر الإمكان.

---

### القسم السادس: بروتوكول التنفيذ العام (يطبَّق على كل TODO أعلاه)

11. **TODO-11: الالتزام ببروتوكول التنفيذ الحتمي**

    * نفّذ التعليمات بالترتيب من TODO-1 حتى TODO-10 دون القفز بينها عشوائيًا.
    * في كل خطوة:

      * أكمل تعديل ملف/مجموعة ملفات بالكامل قبل الانتقال إلى الملف التالي.
      * لا تنشئ أي جدول زمني ولا تقترح خطة زمنية؛ التنفيذ مباشر فقط.
      * عند مواجهة خطأ:

        * لا تتجاهله ولا تؤجله.
        * حلّه من جذوره في نفس السياق قبل المتابعة.
    * اعتبر المهمة الكبرى "مكتملة" فقط عندما تتحقق الشروط:

      * نجاح بناء الواجهة الأمامية بدون أخطاء.
      * نجاح ترجمة backend بـ `tsc --noEmit` بدون أخطاء.
      * وجود ملف `FIXES_APPLIED.md` محدث وواضح.


      * **التنفيذ المستمر:** ابدأ فورًا في تنفيذ المهمة. واصل العمل بشكل متدفق، منفذًا أكبر عدد ممكن من الخطوات بشكل متسلسل  
      ضمن استجابة واحدة   .  
      * **المراقبة والتوقف الإجباري:** راقب طول استجابتك باستمرار. **قبل الوصول إلى الحد الأقصى للتوكنز (Tokens) مباشرةً**، توقف 
      عن العمل للحفاظ على سلامة السياق.

      * **تقرير الحالة عند التوقف:** عند التوقف، يجب أن تنهي ردك **بالصيغة الدقيقة التالية فقط**:

    `[تم الوصول إلى الحد الأقصى للرد. توقفت عند: (اذكر بوضوح آخر موضوع ودرس فرعي عملت عليه). جاهز للمتابعة عند تلقي الأمر.]`
      * **استئناف العمل:** لا تستكمل المهمة إلا بعد أن يرسل لك المستخدم أمرًا صريحًا مثل "أكمل" أو "استمر". عند الاستئناف، ابدأ 
      مباشرة من النقطة التي توقفت عندها دون إعادة أي محتوى سابق.

      * **الإعلان عن الاكتمال:** عندما تكتمل المهمة بالكامل، أنهِ ردك الأخير **فقط** بالعبارة التالية:

    `[اكتملت المهمة بنجاح.]`
---
